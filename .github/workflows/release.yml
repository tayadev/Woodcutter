name: Create Archive
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Upload Archive
      uses: actions/upload-artifact@v3
      with:
        name: woodcutter
        path: /home/runner/work/Woodcutter/Woodcutter